<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="at.ac.univie.hci.MyA3App">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LastSave"
            android:label="@string/title_activity_last_save"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".SpelledSimilarly"
            android:label="@string/title_activity_spelled_similarly"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SoundLike"
            android:label="@string/title_activity_sound_like"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Rhyme"
            android:label="@string/title_activity_rhyme"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>